.modal-dialog.modal-lg
  .modal-content
    .modal-header
      %button.close{:data => {:dismiss => "modal"}}
        x
      %h3.modal-title
        = @maintenance_service_order.new_record? ? 'New Maintenance Item' : 'Edit Maintenance Item'
    .modal-body
      = render(:partial => 'shared/form_errors', :locals => {:obj => @maintenance_service_order}) unless @maintenance_service_order.errors.empty?

      = render partial: "maintenance_service_orders/form"