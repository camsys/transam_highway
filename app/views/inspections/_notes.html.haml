%legend
  Inspection Notes
.row
  .col-sm-6
    = editable_field_tag(@inspection, :notes, 'Inspection Notes', model_name: 'Inspection', type: 'textarea', required: false)
  .col-sm-6
    .row
      = editable_field_tag(@inspection, :temperature, 'Temperature', model_name: 'Inspection', type: 'number', required: false)
    .row
      = editable_field_tag(@inspection, :weather, 'Weather', model_name: 'Inspection', required: false)