.modal-dialog.modal-md
  .modal-content
    .modal-header
      %button.close{:data => {:dismiss => "modal"}}
        x
      %h3.modal-title
        = 'Add File'
    .modal-body
      = render(:partial => 'shared/form_errors', :locals => {:obj => @document}) unless @document.errors.empty?

      = render "form"