.modal-dialog.modal-lg
  .modal-content
    .modal-header
      %button.close{:data => {:dismiss => "modal"}}
        x
      %h3.modal-title
        = @roadway.new_record? ? 'New Roadway' : 'Edit Roadway'
    .modal-body
      = render(:partial => 'shared/form_errors', :locals => {:obj => @roadway}) unless @roadway.errors.empty?

      = render partial: "roadways/form"